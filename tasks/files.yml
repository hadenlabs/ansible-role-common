---
- name: Create files common
  become: true
  file:
    path: '{{ item.path }}'
    state: '{{ item.state }}'
    owner: '{{ item.owner }}'
  with_items: '{{ _common_create_files | default({}) }}'
  when: _common_create_files is defined
